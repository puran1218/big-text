import SwiftUI

enum BigTextColors {
    static let appBackground = Color(red: 0.02, green: 0.02, blue: 0.02) // #050505
    static let surface = Color(red: 0.07, green: 0.07, blue: 0.07) // #111111
    static let textPrimary = Color.white
    static let textSecondary = Color.white.opacity(0.55)
    static let textDisabled = Color.white.opacity(0.28)
    static let controlBackground = Color.white.opacity(0.10)
    static let controlBackgroundPressed = Color.white.opacity(0.16)
}
